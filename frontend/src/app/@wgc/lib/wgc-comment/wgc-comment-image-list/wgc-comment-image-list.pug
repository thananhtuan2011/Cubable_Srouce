.wgc-comment-image(
	*ngFor='let image of images | slice : 0 : 3; let index = index;',
	[lazyLoad]='image[ "@previewUrl" ]',
	[class.wgc-comment-image--more]='index === 2 && images?.length > 3',
	[attr.data-more-count]='images?.length - 3',
	(click)='emitImageClicked( index )')
